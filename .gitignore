node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# Convex tsc artifacts (Convex bundles with esbuild; tsc must not emit)
convex/*.js
convex/*.js.map

# Agent artefacts live in ~/code/aisandbox/life-os/, never in this repo
reports/
debriefs/
plans/
inquiries/
proofs/
