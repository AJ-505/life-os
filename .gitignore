node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# Convex tsc artifacts (Convex bundles with esbuild; tsc must not emit)
convex/*.js
convex/*.js.map

# Convex codegen output — regenerated by `convex dev` / `convex codegen`
convex/_generated/
